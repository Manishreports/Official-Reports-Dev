$('addAllowedLocation').onclick=()=>{let v=N($('allowedLocationInput').value).toUpperCase();if(v&&!allowedLocations.map(NK).includes(NK(v)))allowedLocations.push(v);$('allowedLocationInput').value='';drawAllowedLocations();refresh();save()};
function drawAllowedLocations(){let b=$('allowedLocationTable').querySelector('tbody');b.innerHTML='';allowedLocations.forEach((v,i)=>{let tr=document.createElement('tr'),td=document.createElement('td');td.textContent=v;tr.appendChild(td);tr.appendChild(delbtn(()=>{allowedLocations.splice(i,1);drawAllowedLocations();refresh();save()}));b.appendChild(tr)})}
