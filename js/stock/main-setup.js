$('addSetup').onclick=()=>{setup.push({Order:setup.length+1,Remarks:'',Show:'Yes',PlanSource:'No Plan',Previous:''});drawSetup();refresh();save()};
function drawSetup(){let b=$('setupTable').querySelector('tbody');b.innerHTML='';setup.sort((a,b)=>Q(a.Order)-Q(b.Order)).forEach((r,i)=>{let tr=document.createElement('tr');['Order','Remarks','Show','PlanSource','Previous'].forEach(c=>{let td=document.createElement('td'),el;if(c==='Show'||c==='PlanSource'){el=document.createElement('select');(c==='Show'?['Yes','No']:['No Plan','Planning Database','Previous Negative Pending']).forEach(o=>{let op=document.createElement('option');op.value=o;op.textContent=o;if(o===r[c])op.selected=true;el.appendChild(op)})}else{el=document.createElement('input');el.value=r[c]||''}el.onchange=el.oninput=()=>{setup[i][c]=el.value;save()};td.appendChild(el);tr.appendChild(td)});tr.appendChild(delbtn(()=>{setup.splice(i,1);drawSetup();refresh();save()}));b.appendChild(tr)})}
